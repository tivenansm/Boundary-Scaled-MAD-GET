Each csv file contains boundary lines from a specified year 1960-1989. The columns of the file include the longitude (lon;  float), latitude (lat; float), and the type of boundary; primary boundary line ("Des_Line" string) or  the secondary boundary line ("Non_Line"; string). Other information include the geometry (longitude, latitude as  point) and the name  which is what country the point fall in. 
